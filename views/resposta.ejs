<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Status do Login</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: "Inter", "Segoe UI", sans-serif;
            background: linear-gradient(180deg, #0f111a, #181b25);
            color: #e4e6eb;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 20px;
        }

        .container {
            background: rgba(255, 255, 255, 0.05);
            padding: 40px;
            border-radius: 10px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            max-width: 500px;
            width: 100%;
        }

        .status-icon {
            font-size: 4rem;
            margin-bottom: 20px;
        }

        h1 {
            color: <%- cor %>;
            margin-bottom: 20px;
            font-size: 2rem;
        }

        .mensagem {
            font-size: 1.1rem;
            line-height: 1.6;
            margin-bottom: 30px;
            color: #c9c9c9;
        }

        .botoes {
            display: flex;
            gap: 15px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            padding: 12px 25px;
            border: none;
            border-radius: 5px;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .btn-primario {
            background: #6ab7ff;
            color: #0f111a;
        }

        .btn-secundario {
            background: rgba(255, 255, 255, 0.1);
            color: #e4e6eb;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="status-icon">
            <% if (status === 'sucesso') { %>
                ✅
            <% } else { %>
                ❌
            <% } %>
        </div>
        
        <h1><%= status === 'sucesso' ? 'Sucesso!' : 'Erro!' %></h1>
        
        <div class="mensagem">
            <%- mensagem %>
        </div>
        
        <div class="botoes">
            <% if (status === 'sucesso') { %>
                <a href="/Projects.html" class="btn btn-primario">Ver Projetos</a>
            <% } else { %>
                <a href="/login" class="btn btn-primario">Tentar Novamente</a>
            <% } %>
            <a href="/" class="btn btn-secundario">Página Inicial</a>
            <a href="/cadastra" class="btn btn-secundario">Cadastrar</a>
        </div>
    </div>
</body>
</html>